spring.application.name=config-consumer-service

# target URI for config server corresponds to Kubernetes Service config-service
spring.cloud.config.uri=http://config-service

# client project profile is used to derive which version
# of the properties is served up by the client API
spring.cloud.config.profile=uat

# the label must correspond with the branch name in Github
spring.cloud.config.label=main